---
milestone: v1.0
audited: 2026-06-23
status: tech_debt
scores:
  requirements: 40/40
  phases: 5/5
  integration: 6/6
  flows: 7/7
gaps:
  requirements: []   # none unsatisfied — FAIL gate not triggered
  integration: []    # 0 blockers
  flows: []          # no E2E flow breaks at any traced step
tech_debt:
  - phase: 01-safe-local-round-trip
    items:
      - "Manual UAT open (01-UAT.md status=testing): UAT-1 GUI detect/add game, UAT-2 GUI install→deploy→purge round-trip, UAT-3 in-game Proton load (DEPLOY-08 case-correctness), UAT-4 Flatpak/Snap Steam packaging. All require a display + real Steam/Proton + a Bethesda game; headless engine equivalents are fully tested. UAT-2's round-trip was effectively exercised on real Skyrim (surfaced GAP-01, since resolved by Plan 01-07)."
  - phase: 02-multi-mod-management
    items:
      - "WR-02: profile-switch failure path (post-purge deploy/plugins step fails) reasoned-through but not exercised by an automated failure-injection test."
      - "WR-05: plugins.txt write-failure path (DB rows must NOT persist) reasoned-through but not exercised by an automated write-failure-injection test. (Write-before-persist ordering confirmed by integration checker at plugins.rs:184-201.)"
  - phase: 04-guided-installers-collections
    items:
      - "Manual UAT open (04-UAT.md status=testing): live FOMOD wizard click-through in cargo tauri dev; live Premium NexusMods Collection end-to-end (fetch→bulk-download→deploy→in-game launch→uninstall). Require WebView + live Premium account; headless engine (resolve/validate/visible-step, pristine round-trip) is verified by corpus tests."
      - "GraphQL collectionRevision.downloadLink archive-fetch network seam intentionally not implemented — adapters consume an already-fetched manifest (documented design boundary)."
  - phase: cross-phase
    items:
      - "Integration warning: download_collection partial-failure surface (collections.rs:108-150) — bulk download collects per-mod Results into a report; off-Nexus/failed mods become manual/error rows and deploy then operates on whatever succeeded. Confirm the UI blocks deploy on required-but-missing mods."
      - "Integration note: recover_on_launch is best-effort/non-fatal by design (lib.rs:69) — a game that errors during startup recovery serves the UI mid-journal; the journal replays on next deploy. Acceptable per crash-safety model; flagged for awareness."
      - "Traceability cosmetic: SUMMARY.md requirements_completed frontmatter is sparsely populated; per-requirement coverage is authoritative in the phase VERIFICATION.md tables, not the SUMMARY frontmatter."
---

# Milestone v1.0 — Audit

**Audited:** 2026-06-23
**Status:** `tech_debt` — no critical blockers; all requirements functionally satisfied; accumulated deferred items (manual UAT + 2 untested failure paths + 1 UI-confirmation warning) warrant a conscious decision before close.

## Scope

NexTwist v1.0 — 5 phases, all complete on disk and in ROADMAP.md:

| Phase | Name | Disk | Roadmap |
|-------|------|------|---------|
| 1 | Safe Local Round-Trip | complete | complete |
| 2 | Multi-Mod Management | complete | complete |
| 3 | NexusMods Login & Download | complete | complete |
| 4 | Guided Installers & Collections | complete | complete |
| 5 | AppImage Distribution | complete | complete |

Definition of done (ROADMAP): the full safety round-trip (detect Proton game → install local mod → deploy non-destructively → purge byte-for-byte pristine), then multi-mod conflicts/load-order/profiles, NexusMods login + download, FOMOD + Collections, packaged as a license-clean AppImage. All delivered.

## Phase Verifications

| Phase | VERIFICATION status | Automated score | Notes |
|-------|--------------------|-----------------|-------|
| 1 | human_needed | 31/31 must-haves | GAP-01 (purge orphan empty dirs) RESOLVED by Plan 01-07; 4 manual UAT items open |
| 2 | human_needed | 21/21 must-haves | WR-02/WR-05 failure paths not automated; in-game launch deferred |
| 3 | passed | 14/14 (11 auto + 3 live UAT) | Live OAuth/API-key, Premium download, nxm:// verified on real HW 2026-06-21 |
| 4 | human_needed | 11/11 automatable | 2 live UI/Collection UAT items open |
| 5 | passed | 9/9 must-haves | AppImage durable-Exec + nxm:// routing verified on real HW (per git log) |

No phase reported `gaps_found`. No phase is missing VERIFICATION.md.

## Requirements Coverage (3-source cross-reference)

- **REQUIREMENTS.md traceability:** 40/40 v1 requirements mapped to phases, all `[x]` Complete (100%).
- **Phase VERIFICATION.md tables:** all requirements verified at full automated scores; zero `gaps_found`.
- **Integration map:** every requirement group resolves to a WIRED cross-phase path (see below).

**Final status: 40/40 satisfied.** No `unsatisfied` or `orphaned` requirements — FAIL gate not triggered.

Cosmetic note: SUMMARY.md `requirements_completed` frontmatter is under-populated (many plans leave it blank). This is a documentation-traceability gap only; the VERIFICATION.md requirement tables are authoritative and complete.

## Cross-Phase Integration (integration checker)

All 6 critical seams WIRED with file:line evidence, **0 blockers**:

1. Download (P3) / FOMOD (P4) → staging → deploy engine (P1) — single shared `extract::install_archive` → `store.add_mod` → ordinary `ManagedMod` → one deploy path.
2. Conflict resolution + profiles (P2) → deploy (P1) — `conflict::resolve` → `redeploy_winners`, enabled-only + persisted rank, per-profile rank on switch.
3. Plugin load order (P2) → Proton-prefix AppData `plugins.txt` — `appdata_local_path`, write-before-persist (WR-05 ordering confirmed).
4. Collections (P4) compose FOMOD + load-order + deploy + reversible purge — `deploy_collection` → `switch_profile`; `uninstall_collection` → `purge` before `delete_profile`.
5. Startup recovery before UI — `recover_all_on_launch` inside `.setup()` before window serve.
6. nxm:// single-instance forwarding + deep-link → download — plugin order correct (single-instance before deep-link); `on_open_url` → `handle_nxm_url` → same stage+persist core.

IPC contract: 33 registered commands ↔ 33 frontend invokes, bidirectional match, zero orphaned/broken. `handle_nxm_url` correctly excluded from `generate_handler!` (deep-link router, not a command).

**Non-blocking warnings:** (1) `download_collection` partial-failure → deploy operates on whatever succeeded; confirm UI blocks deploy on required-but-missing mods. (2) `recover_on_launch` best-effort/non-fatal by design.

## E2E Flows

Local round-trip · multi-mod conflict deploy · Nexus download→deploy→purge · FOMOD install · Collection apply→uninstall · nxm:// one-click · crash recovery — **none break at any traced step.**

## Deferred Items Requiring Review (the `tech_debt` bucket)

These are NOT code blockers — the headless safety engine is fully tested and every requirement is functionally satisfied. They are items that inherently require the user's own hardware or that were consciously deferred:

1. **Open manual UAT (Phase 01 — 4 items, Phase 04 — 2 items).** In-game Proton mod load, GUI deploy/purge, Flatpak/Snap detection, live FOMOD wizard, live Premium Collection end-to-end. Require display + real Steam/Proton + Bethesda game + Premium NexusMods account — cannot be executed autonomously.
2. **Two untested failure paths (Phase 02 — WR-02, WR-05).** Reasoned-through, happy paths green, failure-injection tests not written.
3. **One UI-confirmation warning (collection partial-failure deploy gating).**

## Conclusion

Milestone v1.0 is functionally complete, fully integrated, and free of critical blockers. The remaining work is deferred manual UAT and minor test/UI hardening — none of which gate code correctness, but all of which the milestone owner should consciously accept (or schedule) before archiving.
