---
phase: 01-safe-local-round-trip
plan: 05
type: execute
wave: 4
depends_on:
  - 01-02
  - 01-04
files_modified:
  - crates/deploy/src/casefold.rs
  - crates/deploy/src/verify.rs
  - crates/deploy/src/engine.rs
  - crates/deploy/src/lib.rs
  - crates/deploy/tests/casefold_normalize.rs
  - crates/deploy/tests/verify_drift.rs
autonomous: true
requirements:
  - DEPLOY-07
  - DEPLOY-08
  - ENV-04
user_setup: []

must_haves:
  truths:
    - "A mixed-case mod path (e.g. TEXTURES/Foo.DDS) is normalized against the game's canonical Data/ directory casing at deploy time so it loads under Wine"
    - "A verify/repair pass detects manifest-vs-disk drift: orphans (on disk, not in manifest), missing (in manifest, not on disk), and changed (hash differs)"
    - "verify/repair auto-runs after an abnormal exit and reports a clear status without blindly deleting unmanaged files"
    - "Deploy surfaces an unsafe-filesystem warning (cross-device/EXDEV or non-casefolded target) before linking"
  artifacts:
    - path: "crates/deploy/src/casefold.rs"
      provides: "Normalize mod path casing vs the per-game canonical Data/ casing map"
      contains: "casing"
    - path: "crates/deploy/src/verify.rs"
      provides: "Hash-diff manifest-vs-disk drift detection (orphan/missing/changed) + repair"
      contains: "orphan"
    - path: "crates/deploy/tests/casefold_normalize.rs"
      provides: "Mixed-case mod path normalized to canonical Data/ casing"
      contains: "normalize"
    - path: "crates/deploy/tests/verify_drift.rs"
      provides: "verify/repair detects orphan/missing/changed files"
      contains: "drift"
  key_links:
    - from: "crates/deploy/src/casefold.rs"
      to: "crates/steam/src/casing.rs"
      via: "casefold consumes the per-game canonical Data/ casing map produced by the steam crate"
      pattern: "CasingMap|casing"
    - from: "crates/deploy/src/engine.rs"
      to: "crates/deploy/src/casefold.rs"
      via: "deploy normalizes each staged file's target relpath before linking"
      pattern: "normalize"
    - from: "crates/deploy/src/verify.rs"
      to: "crates/store/src/manifest.rs"
      via: "verify diffs list_deployed_files against the on-disk Data/ tree by hash"
      pattern: "list_deployed_files"
---

<objective>
Complete the deployment engine's integrity layer on top of Plan 04: case-sensitivity normalization (DEPLOY-08) so mixed-case Bethesda mod paths load under Wine, and a verify/repair pass (DEPLOY-07) that hash-diffs the manifest against the on-disk game tree to detect orphan / missing / changed files and auto-runs after an abnormal exit. Also surface the unsafe-filesystem warning (cross-device/EXDEV or non-casefolded target) to the deploy path so the user is warned before linking (ENV-04 warning half).

Purpose: These two requirements thicken the proven round-trip spine (Plan 04) with the last two safety guarantees from the phase success criteria: criterion #4's drift/verify ("no orphans") and criterion #5's case resolution ("mods load under Proton") + unsafe-config warning. They depend on the engine (Plan 04) and the canonical casing map (Plan 02), and they modify the same `crates/deploy/` source as Plan 04, so this runs after it.

Output: `crates/deploy/src/casefold.rs` and `verify.rs` (plus a deploy-path hook and an unsafe-config warning surfaced through the DeployReport), with `casefold_normalize` and `verify_drift` tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-safe-local-round-trip/01-CONTEXT.md
@.planning/phases/01-safe-local-round-trip/01-RESEARCH.md
@.planning/research/PITFALLS.md
@.claude/CLAUDE.md
@.planning/phases/01-safe-local-round-trip/01-04-SUMMARY.md

<interfaces>
<!-- Consumed from Plan 02 (steam) and Plan 04 (deploy engine). -->
- steam: `canonical_data_casing(install_dir) -> CasingMap` (lowercase-key -> canonical-cased dir component)
- deploy (Plan 04): `deploy()`, `purge()`, `recover_on_launch()`, `probe() -> FsCaps`, `DeployReport`/`PurgeReport`, manifest access via store
- store: `list_deployed_files(appid)`, `vanilla_for(appid, target_rel)`
- testkit: `fake_game_tree`, `snapshot_tree`, `assert_trees_identical`

<!-- Created by THIS plan (consumed by Plan 06 Tauri commands). -->
- `fn normalize_to_canonical(target_rel: &Path, casing: &CasingMap) -> PathBuf`  // rewrite mod path casing to game canonical
- `fn verify(store, game) -> Result<VerifyReport>`  // orphan/missing/changed lists
- `fn repair(store, game) -> Result<RepairReport>`  // re-deploy missing, restore changed, report (do not auto-delete unmanaged orphans)
- `struct VerifyReport { orphans: Vec<PathBuf>, missing: Vec<PathBuf>, changed: Vec<PathBuf>, pristine: bool }`
- DeployReport carries `fs_warnings: Vec<FsWarning>` (CrossDevice, NotCasefolded) so the UI can warn before/at deploy

<!-- API facts pinned from RESEARCH.md. -->
- casefold flag from probe (FS_CASEFOLD_FL) is best-effort (A6); normalize ALWAYS regardless, for portability
- blake3 hashing for the changed-file diff (matches the manifest hash recorded at deploy)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Case-sensitivity normalization against the canonical Data/ casing + deploy hook + unsafe-config warning</name>
  <read_first>
    - .planning/phases/01-safe-local-round-trip/01-RESEARCH.md (§Common Pitfalls Pitfall 4 Wine case-sensitivity — normalize vs per-game canonical casing; §Pattern 2 probe casefold flag; Assumptions A6)
    - .planning/research/PITFALLS.md (Pitfall 5 case-sensitivity; Anti-Pattern 3 ignoring case until it breaks)
  </read_first>
  <files>crates/deploy/src/casefold.rs, crates/deploy/src/engine.rs, crates/deploy/src/lib.rs, crates/deploy/tests/casefold_normalize.rs</files>
  <behavior>
    - normalize_to_canonical(Path("TEXTURES/Foo.DDS"), casing_map_with{textures->Textures}) returns a path whose directory components match the game's canonical casing (e.g. Textures/...), while leaf-file casing is preserved unless a canonical entry exists.
    - A mod path whose directory casing already matches is returned unchanged.
    - deploy() rewrites each staged file's target relpath through normalize_to_canonical (using the game's CasingMap) before choosing a method/linking, so the deployed path matches what Wine will open.
    - deploy() populates DeployReport.fs_warnings with CrossDevice when probe reports cross-device, and NotCasefolded when the target lacks the casefold flag.
  </behavior>
  <action>
    `casefold.rs`: implement `normalize_to_canonical(target_rel, casing: &CasingMap)` — split the relpath into components, and for each DIRECTORY component look up its lowercased form in the canonical casing map; if found, substitute the canonical casing; if not found, keep the mod's casing (a new mod-introduced dir). This rewrites incoming mixed-case Bethesda mod paths to the game's real `Data/` casing so Wine's case-sensitive `open()` resolves them. The CasingMap is produced by `steam::canonical_data_casing` (Plan 02) — this crate only consumes it.

    `engine.rs`: hook normalization into `deploy()` — before resolving the target path for each staged file, run it through `normalize_to_canonical` with the game's casing map. Also populate `DeployReport.fs_warnings` from the existing `probe()` result: push `CrossDevice` when `!caps.same_device` and `NotCasefolded` when the casefold flag is absent (best-effort, A6). These warnings satisfy the ENV-04 "warn about unsafe configurations" half and feed the UI in Plan 06. Add `FsWarning` to `lib.rs` exports.

    `tests/casefold_normalize.rs`: build a fake game `Data/` tree with canonical `Textures/`, `Meshes/`, derive the casing map via `steam::canonical_data_casing`, and assert `normalize_to_canonical` maps `TEXTURES/x.dds` -> `Textures/x.dds`, `meshes/y.nif` -> `Meshes/y.nif`, and leaves an already-canonical or mod-new path unchanged.
  </action>
  <verify>
    <automated>cargo test -p deploy casefold_normalize && cargo test -p deploy --lib</automated>
  </verify>
  <acceptance_criteria>
    - mixed-case directory components are rewritten to the game's canonical casing; mod-new dirs are preserved
    - deploy() normalizes target paths before linking (DEPLOY-08)
    - DeployReport.fs_warnings surfaces CrossDevice and NotCasefolded (ENV-04 warning)
  </acceptance_criteria>
  <done>Mod path casing normalized to canonical Data/ casing at deploy time; unsafe-fs warnings surfaced (DEPLOY-08, ENV-04).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: verify/repair drift detection (orphan/missing/changed) + auto-run after abnormal exit</name>
  <read_first>
    - .planning/phases/01-safe-local-round-trip/01-RESEARCH.md (§Purge & Verify hash-diff manifest vs disk; §Anti-Patterns "purge by directory scan"; §Validation DEPLOY-07 verify_drift)
    - .planning/research/PITFALLS.md (Pitfall 4 verify/repair diffs manifest vs disk, reports orphans/missing; Recovery Strategies orphan files)
  </read_first>
  <files>crates/deploy/src/verify.rs, crates/deploy/src/lib.rs, crates/deploy/tests/verify_drift.rs</files>
  <behavior>
    - verify(store, game) over a clean deployment reports pristine=true with empty orphan/missing/changed lists.
    - Deleting a deployed file on disk makes it appear in `missing`; an unrecorded extra file under Data/ appears in `orphans`; mutating a deployed file's bytes (hash differs from manifest) appears in `changed`.
    - repair re-deploys missing files and restores changed files from staging/vanilla, and REPORTS orphans (files NexTwist did not record) rather than deleting them blindly — unmanaged user/vanilla files are never auto-deleted.
    - recover_on_launch (Plan 04) is extended to invoke verify after journal replay so an abnormal exit yields an automatic drift report.
  </behavior>
  <action>
    `verify.rs`: implement `verify(store, game) -> VerifyReport`. Load `list_deployed_files(appid)`; for each recorded file, check on-disk existence and blake3-hash it, classifying as `missing` (absent) or `changed` (hash != recorded). Walk the game `Data/` tree (walkdir) and classify any file present on disk but NOT in the manifest and NOT a known vanilla original as an `orphan`. `pristine = missing.is_empty() && changed.is_empty() && orphans.is_empty()`. `repair(store, game)` re-deploys `missing` from staging (via the Plan 04 engine path), restores `changed` to the recorded state, and returns the orphan list for the UI to surface — it MUST NOT delete orphans automatically (Pitfall 4: never directory-scan-delete; unmanaged files are reported only).

    Extend `recover_on_launch` (Plan 04, in engine.rs/journal.rs) to call `verify` after journal replay so an abnormal exit produces an automatic drift report (`full_pristine_or_report` per RESEARCH.md Pattern 1).

    `tests/verify_drift.rs`: deploy a mod, assert verify is pristine; then (a) delete a deployed file and assert it is `missing`; (b) drop an extra unrecorded file into Data/ and assert it is an `orphan` (and that repair does NOT delete it); (c) mutate a deployed file's bytes and assert it is `changed`. Confirm repair restores missing+changed and leaves the orphan intact-but-reported.
  </action>
  <verify>
    <automated>cargo test -p deploy verify_drift && cargo test -p deploy --lib</automated>
  </verify>
  <acceptance_criteria>
    - verify classifies orphan / missing / changed correctly and reports pristine on a clean deployment
    - repair restores missing+changed; orphans are reported, never auto-deleted (DEPLOY-07)
    - recover_on_launch runs verify after replay (auto drift report on abnormal exit)
  </acceptance_criteria>
  <done>verify/repair detects manifest-vs-disk drift and auto-runs after abnormal exit, never blindly deleting unmanaged files (DEPLOY-07).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external changes → deployed game tree | The game/Steam/user may change files under Data/ outside NexTwist; verify must detect, not clobber |
| mixed-case mod paths → Wine open() | Case-sensitive Linux fs vs case-insensitive-authored mod paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-16 | Tampering | repair auto-deletes an unmanaged user/vanilla file mistaken for an orphan | mitigate | repair NEVER deletes orphans — it reports them; only manifest-recorded paths are ever removed (Pitfall 4 / Anti-Pattern) |
| T-01-17 | Denial of Service (to the user's game) | silent case mismatch -> mods load with no effect | mitigate | Normalize every mod path to canonical Data/ casing at deploy time; surface NotCasefolded warning (Pitfall 5) |
| T-01-18 | Tampering | undetected external drift leaves stale/orphan state after a crash | mitigate | verify hash-diffs manifest vs disk and auto-runs after abnormal exit; provenance distinguishes ours/vanilla/unmanaged |
</threat_model>

<verification>
- `cargo test -p deploy` green incl. casefold_normalize + verify_drift (and Plan 04 tests still pass).
- Mixed-case mod paths normalized to canonical casing; deploy surfaces fs warnings.
- verify classifies orphan/missing/changed; repair restores managed files and only reports orphans.
</verification>

<success_criteria>
- DEPLOY-08: case-sensitivity mismatches resolved so mods load under Proton.
- DEPLOY-07: verify/repair detects manifest-vs-disk drift (orphan/missing/changed) and auto-runs after abnormal exit.
- ENV-04 (warning half): unsafe-fs (cross-device / non-casefolded) warnings surfaced through DeployReport.
</success_criteria>

<output>
Create `.planning/phases/01-safe-local-round-trip/01-05-SUMMARY.md` when done.
</output>
