---
phase: 06-starfield-detection-ce2-path-resolution
plan: 03
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - src-tauri/src/commands/games.rs
  - src-tauri/src/lib.rs
  - frontend/src/lib/api.ts
  - frontend/src/routes/+page.svelte
autonomous: true
requirements: [SFDET-01, SFDET-02, SFDET-03]
must_haves:
  truths:
    - "The Starfield game view shows an actionable 'launch Starfield once via Steam, then re-check' guidance plus a manual Re-check action while the CE2 state is FirstLaunchPending, and blocks load-order/INI actions until Ready — the game is still addable/detectable (SFDET-02)."
    - "The view shows a persistent, non-blocking notice with an installed-vs-validated build line ONLY when the installed build is newer than the validated baseline (SFDET-03)."
    - "Detection/logic stays headless in steam; the new Tauri command forwards the typed StarfieldStatus verbatim and the UI never blocks management on drift (thin adapter — engine-boundary rule)."
  artifacts:
    - src-tauri/src/commands/games.rs
    - src-tauri/src/lib.rs
    - frontend/src/lib/api.ts
    - frontend/src/routes/+page.svelte
  key_links:
    - "starfield_status command re-resolves the Steam library + Proton prefix for the appid (paths can move — never cached) then returns steam::starfield_status(...) with no adapter logic."
    - "Re-check re-invokes starfield_status on explicit user action only — no write-on-focus, no surprise writes (Phase 6 writes nothing to the game/prefix)."
---

<objective>
Surface Phase 6's headless detection to the user (SFDET-01/02/03): a thin Tauri
`starfield_status` command that forwards the `steam::StarfieldStatus` aggregate, and a
Starfield game-view treatment that renders the first-launch guidance + Re-check action
(blocking load-order/INI actions until Ready) and the persistent version-drift notice.

Purpose: Close the user-visible half of SFDET-02 (first-launch guidance, per the
First-Launch-Not-Done decision) and SFDET-03 (drift notice, per the Version-Drift
decision), keeping ALL logic in the engine — the adapter and UI only present the typed
state. The real in-app visual confirmation on a live Proton install is Phase 9's
on-hardware gate; Phase 6's automated gate is svelte-check + build + the engine tests.
Output: a registered `starfield_status` command, its typed `api.ts` wrapper, and the
Starfield game-view notice/guidance in `+page.svelte`.

Depends on Plan 01 (steam `starfield_status`/`Ce2ConfigState`/`DriftNotice`) and Plan 02
(loadorder allow-list arms) → Wave 2.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-starfield-detection-ce2-path-resolution/06-RESEARCH.md
@.planning/phases/06-starfield-detection-ce2-path-resolution/06-CONTEXT.md
@.planning/phases/06-starfield-detection-ce2-path-resolution/06-01-PLAN.md
</context>

## Artifacts this plan produces (new symbols)

- `src-tauri/src/commands/games.rs`: `#[tauri::command] fn starfield_status(state, appid) -> Result<steam::StarfieldStatus, String>` (thin).
- `src-tauri/src/lib.rs`: `starfield_status` added to the `generate_handler!` list.
- `frontend/src/lib/api.ts`: `starfieldStatus(appid)` typed `invoke` wrapper + the `StarfieldStatus`/`Ce2ConfigState`/`DriftNotice` TS types.
- `frontend/src/routes/+page.svelte`: Starfield first-launch guidance + Re-check + persistent drift notice; load-order/INI actions gated on `Ready`.

<tasks>

<task type="auto">
  <name>Task 1: Thin Tauri starfield_status command + handler registration</name>
  <files>src-tauri/src/commands/games.rs, src-tauri/src/lib.rs</files>
  <read_first>
    - src-tauri/src/commands/games.rs:17 — `detect_games`; :23 — `add_game` (how a command resolves the Steam library/prefix for an appid and locks `AppState`).
    - src-tauri/src/lib.rs:21 — `resolve_data_dir`; :129 — `.invoke_handler(tauri::generate_handler![ … ])`.
    - crates/steam/src/ce2.rs — `starfield_status`/`StarfieldStatus` from Plan 01 (the value to forward).
  </read_first>
  <action>
    Add a `#[tauri::command] fn starfield_status(state, appid) -> Result<steam::StarfieldStatus, String>`
    to games.rs following the existing `add_game`/`detect_games` shape: resolve the Steam
    library root + Proton prefix for `appid` the same way the existing detection commands do
    (re-resolve — never cache paths, per the resolve.rs header), then return
    `steam::starfield_status(prefix, library_root, appid)` mapped to `String` errors at the
    boundary (anyhow/`.map_err(|e| e.to_string())`). Keep it a thin forwarder — NO detection,
    path, case-fold, or drift logic in the adapter (engine-boundary hard rule); if the prefix
    cannot be resolved, surface the same error the existing add-game/detect path already returns.
    Register `commands::games::starfield_status` in the `generate_handler!` list in lib.rs. Do not
    add a checkpoint — the engine logic is already tested in Plan 01; this task is compile-gated.
  </action>
  <verify>
    <automated>cargo build -p nextwist --locked && cargo test -p nextwist-steam --locked starfield_status</automated>
  </verify>
  <acceptance_criteria>
    - Source: `starfield_status` exists in games.rs, returns `steam::StarfieldStatus`, and is present in the `generate_handler!` list; the body contains no path/case-fold/drift logic (forwards `steam::starfield_status`).
    - Behavior: `src-tauri` compiles with the command registered; the forwarded `StarfieldStatus` serializes for IPC (serde derive from Plan 01).
    - Test: `cargo build -p nextwist` succeeds; `cargo test -p nextwist-steam starfield_status` green.
  </acceptance_criteria>
  <done>A thin `starfield_status` command forwards the engine aggregate and is registered in the Tauri handler; the shell compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend Starfield game view — first-launch guidance + Re-check + drift notice</name>
  <files>frontend/src/lib/api.ts, frontend/src/routes/+page.svelte</files>
  <read_first>
    - frontend/src/lib/api.ts:388-395 — `detectGames`/`addGame`/`listGames` invoke-wrapper pattern + the `DetectedGame`/`Game` TS types.
    - frontend/src/routes/+page.svelte — the existing game view / action layout the notice + gating attaches to.
  </read_first>
  <action>
    Add a `starfieldStatus(appid): Promise<StarfieldStatus>` wrapper to api.ts mirroring the
    existing `addGame` invoke pattern (`invoke("starfield_status", { appid })`), plus TS types for
    `StarfieldStatus`, `Ce2ConfigState` (a Ready/FirstLaunchPending tagged shape matching the serde
    representation from Plan 01), and `DriftNotice`. In +page.svelte, for the Starfield game view:
    when the CE2 state is FirstLaunchPending, render the actionable guidance "Launch Starfield once
    via Steam so it creates its config folder, then re-check" (per the First-Launch-Not-Done
    decision — do NOT attempt to auto-launch the Proton game) with a manual "Re-check" button that
    re-invokes `starfieldStatus` on explicit click only (no write-on-focus), and DISABLE the
    load-order / INI-management actions while pending; when Ready, enable them. Independently, when
    `drift` is present (installed newer than validated), show a persistent, non-intrusive notice on
    the Starfield game view with an "installed build N vs validated build M" line (per the
    Version-Drift decision — advisory/non-blocking; it must NEVER disable management). Render build
    numbers as plain text (no HTML injection). Keep wording concise (message wording is Claude's
    discretion per CONTEXT).
  </action>
  <verify>
    <automated>npm --prefix frontend run check && npm --prefix frontend run build</automated>
  </verify>
  <acceptance_criteria>
    - Source: `starfieldStatus` + `StarfieldStatus`/`Ce2ConfigState`/`DriftNotice` types in api.ts; +page.svelte gates load-order/INI actions on `Ready`, renders first-launch guidance + a Re-check button, and renders the drift notice only when `drift` is present.
    - Behavior: FirstLaunchPending → guidance + Re-check shown, management actions disabled; Ready → actions enabled; drift present → persistent installed-vs-validated line shown and management NOT blocked by it.
    - Test: `npm --prefix frontend run check` (svelte-check) passes and `npm --prefix frontend run build` succeeds.
  </acceptance_criteria>
  <done>The Starfield game view surfaces first-launch guidance + Re-check (gating management until Ready) and a persistent advisory drift notice; svelte-check and build pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| steam engine → Tauri IPC → frontend | The typed `StarfieldStatus` (state path + build numbers, derived from attacker-influenceable prefix contents) crosses IPC into the webview. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04 | Tampering/EoP | `starfield_status` adapter | low | mitigate | Adapter forwards the engine value verbatim; all path/traversal validation already done in `steam` (T-06-01/02). No path construction or FS write in the command. |
| T-06-05 | Information disclosure / injection | Rendering build numbers + paths in the webview | low | mitigate | Render `installed`/`validated` build numbers and the state path as plain text (Svelte escapes by default) — no `{@html}`, no eval of prefix-derived strings. |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | No dependencies added in Phase 6 (no new npm or crate deps; verified in RESEARCH § Package Legitimacy Audit) — supply-chain surface unchanged. |
</threat_model>

<verification>
- `cargo build -p nextwist --locked` succeeds with `starfield_status` registered.
- `npm --prefix frontend run check` (svelte-check) passes; `npm --prefix frontend run build` succeeds.
- `cargo clippy -p nextwist --all-targets -- -D warnings` clean (src-tauri workspace member; requires WebKitGTK dev libs on the host).
- Adapter contains no engine logic (forwards `steam::starfield_status`); real in-app visual verification is deferred to Phase 9 (on-hardware).
</verification>

<success_criteria>
- The user can add/detect Starfield and, before first launch, sees actionable guidance + a Re-check action with load-order/INI management blocked until Ready (SFDET-02 UI half).
- A persistent, non-blocking installed-vs-validated drift notice appears only when the installed build is newer (SFDET-03).
- All logic stayed headless; the adapter is thin and the UI is presentation-only (SFDET-01 end-to-end).
</success_criteria>

<output>
Create `.planning/phases/06-starfield-detection-ce2-path-resolution/06-03-SUMMARY.md` when done.
</output>
