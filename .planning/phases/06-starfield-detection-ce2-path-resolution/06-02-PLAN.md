---
phase: 06-starfield-detection-ce2-path-resolution
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/loadorder/assets/starfield/masterlist.yaml
  - crates/loadorder/src/masterlist.rs
  - crates/loadorder/src/loot.rs
  - crates/loadorder/src/scan.rs
autonomous: true
requirements: [SFDET-01]
must_haves:
  truths:
    - "game_type_for(1716740)=Some(GameType::Starfield), game_id_for(1716740)=Some(GameId::Starfield), game_slug(1716740)=Some(\"starfield\"), appdata_folder_name(1716740)=Some(\"Starfield\"); junk appids (0, 220) still return None (SFDET-01)."
    - "bundled_snapshot(1716740) returns a non-empty bundled Starfield masterlist; the loadorder crate compiles (the include_str! target physically exists)."
  artifacts:
    - crates/loadorder/assets/starfield/masterlist.yaml
    - crates/loadorder/src/masterlist.rs
    - crates/loadorder/src/loot.rs
    - crates/loadorder/src/scan.rs
  key_links:
    - "The STARFIELD_SNAPSHOT include_str! target (assets/starfield/masterlist.yaml) MUST exist before masterlist.rs compiles — the asset is created first (Wave 0)."
    - "MASTERLIST_BRANCH \"v0.29\" already matches loot/starfield@v0.29 — the appid-generic masterlist_url builder needs only a game_slug arm, no URL change."
---

<objective>
Complete Starfield's allow-list surface in the headless `loadorder` engine and bundle its
LOOT masterlist snapshot (SFDET-01). These arms are consumed by Phase 7 (load order) and
Phase 8, but they belong to Phase 6's detection surface: the game must be fully allow-listed
across `game_type_for`/`game_id_for`/`game_slug`/`appdata_folder_name`, and the bundled
masterlist asset must physically exist or the crate will not compile.

Purpose: Round out the ~6-arm allow-list (the `steam` half is Plan 01) so Starfield is a
first-class managed Bethesda game, and ship the offline-first masterlist snapshot matching
the v1.0 pattern (per the Detection Scope & Masterlist decision: bundle at build time).
Output: `crates/loadorder/assets/starfield/masterlist.yaml` (new bundled CC0 snapshot) +
Starfield arms in `masterlist.rs`/`loot.rs`/`scan.rs`.

This plan shares NO files with Plan 01 (steam/testkit) → both run in Wave 1, in parallel.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-starfield-detection-ce2-path-resolution/06-RESEARCH.md
@.planning/phases/06-starfield-detection-ce2-path-resolution/06-PATTERNS.md
</context>

## Artifacts this plan produces (new symbols)

- `crates/loadorder/assets/starfield/masterlist.yaml` — bundled CC0 snapshot of `loot/starfield@v0.29`.
- `masterlist.rs`: mirror `const STARFIELD: u32`; `const STARFIELD_SNAPSHOT = include_str!("../assets/starfield/masterlist.yaml")`; `game_slug` Starfield arm ("starfield"); `bundled_snapshot` Starfield arm.
- `loot.rs`: mirror `const STARFIELD`; `game_type_for` arm (`GameType::Starfield`); `appdata_folder_name` arm ("Starfield" — Phase 7 plugins.txt lives in AppData/Local).
- `scan.rs`: mirror `const STARFIELD`; `game_id_for` arm (`GameId::Starfield`).

<tasks>

<task type="auto">
  <name>Task 1 (Wave 0 asset): bundle the Starfield masterlist + masterlist.rs slug/snapshot arms</name>
  <files>crates/loadorder/assets/starfield/masterlist.yaml, crates/loadorder/src/masterlist.rs</files>
  <read_first>
    - crates/loadorder/src/masterlist.rs:28 — mirror-const convention; :34 — `MASTERLIST_BRANCH = "v0.29"`; :42-43 — `SKYRIMSE_SNAPSHOT`/`FALLOUT4_SNAPSHOT` include_str!; :46 — `game_slug`; :55-58 — `bundled_snapshot`; :65 — `masterlist_url` builder; :260 — `falls_back_to_bundled_snapshot_when_offline`.
    - crates/loadorder/tests/plugins.rs:182 — `bundled_snapshot_is_present_and_nonempty`.
  </read_first>
  <action>
    Fetch the CC0 LOOT Starfield masterlist snapshot from
    `https://raw.githubusercontent.com/loot/starfield/v0.29/masterlist.yaml` (RESEARCH verified
    branch `v0.29` live, last commit 2026-06-20) and save it verbatim to
    `crates/loadorder/assets/starfield/masterlist.yaml`; confirm it is non-empty valid YAML and
    that the `loot/starfield` repo LICENSE is CC0-1.0 (same basis the SSE/FO4 snapshots are
    bundled under — RESEARCH A4). This asset MUST land before the include_str! below or the crate
    will not compile. Then extend masterlist.rs, mirroring the existing snapshot pattern: add a
    mirrored `const STARFIELD: u32 = 1716740` (matching the per-module mirror-const convention),
    `const STARFIELD_SNAPSHOT: &str = include_str!("../assets/starfield/masterlist.yaml")`, a
    `game_slug` arm returning "starfield", and a `bundled_snapshot` arm returning
    `STARFIELD_SNAPSHOT`. Do NOT touch `MASTERLIST_BRANCH` or `masterlist_url` — `v0.29` already
    matches `loot/starfield@v0.29`, so the appid-generic URL builder works with just the slug arm
    (per the Detection Scope & Masterlist decision to bundle offline-first and reuse the v1.0
    pipeline). Add a bundled-snapshot test for Starfield mirroring `bundled_snapshot_is_present_and_nonempty`.
  </action>
  <verify>
    <automated>cargo test -p nextwist-loadorder --locked bundled_snapshot game_slug starfield</automated>
  </verify>
  <acceptance_criteria>
    - Source: `crates/loadorder/assets/starfield/masterlist.yaml` exists, non-empty; `masterlist.rs` has `STARFIELD_SNAPSHOT` (include_str!), a `game_slug(1716740)=="starfield"` arm, and a `bundled_snapshot(1716740)==Some(STARFIELD_SNAPSHOT)` arm.
    - Behavior: `bundled_snapshot(1716740)` returns `Some(non-empty)`; the crate compiles (include_str! target present).
    - Test: `cargo test -p nextwist-loadorder` green; new Starfield bundled-snapshot test asserts present + non-empty.
  </acceptance_criteria>
  <done>The Starfield masterlist snapshot is bundled and wired through `game_slug`/`bundled_snapshot`; the crate compiles and the snapshot test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Starfield arms in loot.rs (game_type_for + appdata_folder_name) and scan.rs (game_id_for)</name>
  <files>crates/loadorder/src/loot.rs, crates/loadorder/src/scan.rs</files>
  <read_first>
    - crates/loadorder/src/loot.rs:54 — mirror-const; :81 — `game_type_for`; :93 — `appdata_folder_name`; :463 — `game_type_for_allow_lists_only_the_two_supported_games`.
    - crates/loadorder/src/scan.rs:35 — mirror-const; :44 — `game_id_for`.
  </read_first>
  <action>
    Add the Starfield arm to each remaining 2-arm `match appid` allow-list site (values
    HIGH-confidence per RESEARCH; the enum variants `GameType::Starfield` / `GameId::Starfield`
    already exist in the pinned libloot 0.29.5 / esplugin 6.1.4 — no dependency change). In
    loot.rs: add a mirrored `const STARFIELD: u32 = 1716740`, `game_type_for` returns
    `Some(GameType::Starfield)`, and `appdata_folder_name` returns `Some("Starfield")` (Starfield's
    `Plugins.txt` lives in AppData/Local/Starfield — this arm is consumed by Phase 7, not written
    here). In scan.rs: add the mirrored `const STARFIELD` and a `game_id_for` arm returning
    `Some(GameId::Starfield)`. Extend the existing allow-list test to assert THREE supported games
    pass (add the Starfield assertion) while keeping the junk-appid negative assertions (0, 220
    still return `None`) — the point is the allow-list still rejects everything else. Do NOT
    hard-code any Starfield master/medium-master plugin list — libloot owns that in Phase 7.
  </action>
  <verify>
    <automated>cargo test -p nextwist-loadorder --locked allow_lists game_type_for game_id_for</automated>
  </verify>
  <acceptance_criteria>
    - Source: `game_type_for(1716740)==Some(GameType::Starfield)`; `appdata_folder_name(1716740)==Some("Starfield")`; `game_id_for(1716740)==Some(GameId::Starfield)`.
    - Behavior: the extended allow-list test asserts all three games pass and 0/220 return `None`; no plugin list is hard-coded.
    - Test: `cargo test -p nextwist-loadorder` green; `cargo clippy -p nextwist-loadorder --all-targets -- -D warnings` clean.
  </acceptance_criteria>
  <done>loadorder fully allow-lists 1716740 across `game_type_for`/`appdata_folder_name`/`game_id_for`, verified by the extended three-game allow-list test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `loot/starfield` GitHub → bundled asset | The masterlist YAML is fetched once at plan time and compiled in via include_str!; it becomes a build-time trusted artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-02b | Tampering | Bundled masterlist provenance | low | mitigate | Fetch only from the official `loot/starfield@v0.29` raw URL over HTTPS; confirm CC0-1.0 LICENSE; save verbatim (no post-processing). Snapshot is data consumed by libloot in Phase 7, not executable. |
| T-06-03b | DoS | Malformed bundled YAML | low | accept | libloot parses the masterlist in Phase 7 with its own error handling; a non-empty-YAML check at bundle time is sufficient for Phase 6 (no runtime parse here). |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | No dependencies added in Phase 6 (Cargo.toml `[workspace.dependencies]` unchanged; verified in RESEARCH § Package Legitimacy Audit) — supply-chain surface unchanged. The masterlist is a data asset, not a package. |
</threat_model>

<verification>
- `cargo test -p nextwist-loadorder --locked` green (allow-list three-game assertions + Starfield bundled-snapshot).
- `cargo clippy -p nextwist-loadorder --all-targets -- -D warnings` clean.
- `cargo deny check advisories bans licenses sources` still passes (no new deps; the masterlist is a bundled data asset, not a crate).
- No DB migration, no `core` change, no dependency/MSRV bump.
</verification>

<success_criteria>
- Starfield (1716740) is fully allow-listed across the loadorder engine (`game_type_for`, `game_id_for`, `game_slug`, `appdata_folder_name`) — SFDET-01.
- The bundled `assets/starfield/masterlist.yaml` exists and is wired through `bundled_snapshot`; the crate compiles and the offline snapshot is available for Phase 7.
</success_criteria>

<output>
Create `.planning/phases/06-starfield-detection-ce2-path-resolution/06-02-SUMMARY.md` when done.
</output>
