---
phase: 02-multi-mod-management
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/loadorder/Cargo.toml
  - crates/loadorder/src/lib.rs
  - crates/loadorder/src/error.rs
  - crates/loadorder/src/loot.rs
  - crates/loadorder/tests/libloot_spike.rs
  - crates/testkit/src/lib.rs
autonomous: false
requirements: [PLUGIN-01, PLUGIN-02, PLUGIN-03]
user_setup: []

must_haves:
  truths:
    - "A fixture Proton-prefix AppData/Local/<GameName> directory can be built for headless plugins.txt tests"
    - "libloot::Game::with_local_path constructs against the fixture prefix without NoLocalAppData (Linux seam, A1/A3)"
    - "A one-library enable + set_load_order + save round-trip writes an asterisk-format plugins.txt at the libloot-reported path"
    - "The crates/loadorder crate is Tauri-free and compiles in the headless workspace"
  artifacts:
    - path: "crates/loadorder/src/loot.rs"
      provides: "libloot wrapper: with_local_path Game construction + load/sort/set_load_order/save"
      contains: "with_local_path"
    - path: "crates/loadorder/src/error.rs"
      provides: "LoadOrderError thiserror enum (analog of DeployError)"
    - path: "crates/loadorder/tests/libloot_spike.rs"
      provides: "A1/A3 de-risk: with_local_path round-trip against a fixture prefix"
    - path: "crates/testkit/src/lib.rs"
      provides: "fake_proton_prefix helper for headless plugins.txt tests"
      contains: "fake_proton_prefix"
  key_links:
    - from: "crates/loadorder/src/loot.rs"
      to: "libloot crate"
      via: "Game::with_local_path(game_type, install_dir, proton_appdata_local)"
      pattern: "with_local_path"
    - from: "crates/loadorder/tests/libloot_spike.rs"
      to: "crates/testkit/src/lib.rs"
      via: "fake_proton_prefix builds the AppData/Local/<GameName> fixture"
      pattern: "fake_proton_prefix"
---

<objective>
De-risk the single largest technical unknown (RESEARCH Assumptions A1/A3) BEFORE building out the plugin/LOOT slice: stand up the headless `crates/loadorder` crate, add the `libloot` dependency behind a blocking package-legitimacy checkpoint, and prove a minimal `Game::with_local_path → load → set_load_order → save` round-trip writes an asterisk-format plugins.txt at the libloot-reported path against a FIXTURE Proton-prefix AppData dir.

This implements D-09 (integrate libloot, not hand-rolled) and the RESEARCH directive to spike A1/A3 first. It establishes the libloot seam (Pitfall 1: ALWAYS `with_local_path` on Linux) that Plan 04 builds the full plugin manager on. No conflict resolution, no masterlist fetch, no full plugin scan, no UI here — those are Plan 04.

Purpose: prove the libloot Linux integration works against a controllable fixture so Plan 04 is mechanical, and gate the first GPL-3.0 / SUS package install behind a human checkpoint.
Output: loadorder crate scaffold (lib/error), a verified libloot wrapper, a passing spike test, a testkit `fake_proton_prefix` helper.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-multi-mod-management/02-RESEARCH.md
@.planning/phases/02-multi-mod-management/02-PATTERNS.md

# Analog sources (read before writing):
@crates/deploy/src/error.rs
@crates/deploy/Cargo.toml
@crates/steam/src/resolve.rs
@crates/testkit/src/lib.rs
@Cargo.toml
</context>

<artifacts_this_plan_produces>
- New crate `crates/loadorder` (package `nextwist-loadorder`), Tauri-free, workspace member.
- `crates/loadorder/src/error.rs`: `enum LoadOrderError` (thiserror; `#[from] StoreError`, structured `Io { path, source }` + `io()` ctor, `Loot(String)`, `NoLocalAppData`).
- `crates/loadorder/src/loot.rs`: `fn appdata_local_path(prefix: &Path, game_name: &str) -> PathBuf` (builds `<prefix>/drive_c/users/steamuser/AppData/Local/<game_name>`); `fn open_game(game_type, install_dir, appdata_local) -> Result<libloot::Game, LoadOrderError>` (always `with_local_path`); `fn game_type_for(appid: u32) -> Option<libloot::GameType>` (489830→SkyrimSE, 377160→Fallout4); a `set_order_and_save(&mut Game, &[&str])` helper.
- `crates/loadorder/tests/libloot_spike.rs`: the A1/A3 round-trip test.
- `crates/testkit/src/lib.rs`: `pub fn fake_proton_prefix(root, game_name, plugins_txt: Option<&str>) -> io::Result<PathBuf>` returning the prefix root with the AppData/Local/<game_name> tree (and an optional seeded Plugins.txt).
- Cargo.toml workspace: add `loadorder` to internal path deps and `libloot = "0.29"` to `[workspace.dependencies]`.
</artifacts_this_plan_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Package-legitimacy checkpoint before first libloot install</name>
  <action>Pause for the developer to verify the libloot package legitimacy (steps in how-to-verify) BEFORE any libloot dependency is added. Do not auto-advance past this blocking checkpoint; libloot is SUS-on-low-downloads and GPL-3.0-or-later, so this gate is never auto-approvable.</action>
  <what-built>
Nothing on disk yet — this gate is the package-legitimacy checkpoint required BEFORE the first `libloot` dependency install (RESEARCH Package Legitimacy Audit: libloot is flagged SUS on low downloads only, approved as niche/authoritative; planner MUST insert one human-verify before install). libloot is GPL-3.0-or-later, which makes the distributed binary GPL-3.0 (carry to Phase-5 DIST-02).
  </what-built>
  <how-to-verify>
1. Open https://crates.io/crates/libloot — confirm: current version 0.29.x, license `GPL-3.0-or-later`, repository `github.com/loot/libloot` (the official LOOT org), no install scripts, not yanked.
2. Open https://github.com/loot/libloot — confirm it is the LOOT project's pure-Rust libloot port (authored/maintained by Ortham / the LOOT org).
3. Confirm you accept that adding libloot makes the distributed NexTwist binary effectively GPL-3.0-or-later (already NexTwist's own license; deny.toml allows it — Plan 01 Task 1).
  </how-to-verify>
  <resume-signal>Type "approved" to authorize adding `libloot = "0.29"`, or describe a concern (e.g. version drift) to halt.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Scaffold crates/loadorder (Cargo.toml + lib.rs + error.rs) and add libloot dep</name>
  <files>Cargo.toml, crates/loadorder/Cargo.toml, crates/loadorder/src/lib.rs, crates/loadorder/src/error.rs</files>
  <read_first>crates/deploy/Cargo.toml (member manifest layout), crates/deploy/src/error.rs (whole file — copy the thiserror enum + Io ctor exactly), crates/deploy/src/lib.rs (crate-doc + module + pub use style), Cargo.toml (workspace.dependencies + internal path deps)</read_first>
  <action>
Create the new headless crate (D-09; PATTERNS crate-structure analog = crates/deploy / crates/steam). Only proceed after the checkpoint above is approved.

1. Workspace Cargo.toml: under `[workspace.dependencies]` add `libloot = "0.29"`; under the internal-crate path-dep block add `loadorder = { path = "crates/loadorder", package = "nextwist-loadorder" }`. (The `crates/*` members glob already includes the new crate.)
2. `crates/loadorder/Cargo.toml`: copy crates/deploy/Cargo.toml layout. `[package] name = "nextwist-loadorder"`, edition/rust-version/license/repository `.workspace = true`. `[dependencies]`: `core = { workspace = true }` (aliased nextwist-core — match how deploy imports it; use `nextwist_core` if a `core` name shadows std), `store = { workspace = true }`, `thiserror = { workspace = true }`, `tracing = { workspace = true }`, `libloot = { workspace = true }`. `[dev-dependencies]`: `tempfile = { workspace = true }`, `testkit = { workspace = true }`. Do NOT add reqwest here yet (Plan 04 adds masterlist fetch).
3. `crates/loadorder/src/error.rs`: copy crates/deploy/src/error.rs structure — `#[derive(Debug, Error)] pub enum LoadOrderError` with `Store(#[from] StoreError)`, structured `Io { path: PathBuf, #[source] source: std::io::Error }` + `pub(crate) fn io(path, source)` ctor, `#[error("libloot error: {0}")] Loot(String)`, and `#[error("no local AppData path resolved for the Proton prefix: {0}")] NoLocalAppData(PathBuf)`. NEVER use anyhow in this lib crate.
4. `crates/loadorder/src/lib.rs`: crate doc (headless plugin/load-order management via libloot; Tauri-free; ALWAYS with_local_path on Linux — Pitfall 1). `pub mod error; pub mod loot;` and `pub use error::LoadOrderError;`. (scan.rs/masterlist.rs are added in Plan 04.)
  </action>
  <verify>
    <automated>cargo build -p nextwist-loadorder 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `crates/loadorder` is a workspace member that compiles with the libloot dep resolved.
    - `LoadOrderError` mirrors DeployError's shape (thiserror, Io ctor, no anyhow).
    - libloot appears once in `[workspace.dependencies]` at "0.29".
  </acceptance_criteria>
  <done>Headless loadorder crate scaffolded with libloot wired and a thiserror error type; workspace builds.</done>
</task>

<task type="auto">
  <name>Task 2: testkit fake_proton_prefix helper</name>
  <files>crates/testkit/src/lib.rs</files>
  <read_first>crates/testkit/src/lib.rs (whole file — match the fake_game_tree / write_tree helper style + doc-comment conventions)</read_first>
  <action>
Add a `pub fn fake_proton_prefix(root: &Path, game_name: &str, plugins_txt: Option<&str>) -> io::Result<PathBuf>` (Wave-0 fixture gap from VALIDATION: "fixture Proton-prefix AppData dir for headless plugins.txt write tests"). It creates `<root>/drive_c/users/steamuser/AppData/Local/<game_name>/` (mkdir -p), optionally writes `Plugins.txt` with the provided contents inside that folder, and returns `root` (the prefix root, suitable to pass where the steam crate would supply a real prefix). Doc-comment it the same way as `fake_game_tree`: explain it mimics the Proton-prefix AppData/Local/<GameName> location libloot's `with_local_path` targets on Linux (Pitfall 1), so plugins.txt round-trips can be asserted headlessly. Add a small unit test asserting the AppData/Local/<game_name> dir exists and a seeded Plugins.txt is readable back.
  </action>
  <verify>
    <automated>cargo test -p nextwist-testkit fake_proton_prefix 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `fake_proton_prefix` creates the full AppData/Local/<game_name> path and (optionally) seeds Plugins.txt.
    - Unit test confirms the path exists and seeded content round-trips.
  </acceptance_criteria>
  <done>testkit exposes a reusable fixture-prefix builder for headless libloot/plugins.txt tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: libloot wrapper + A1/A3 spike round-trip test</name>
  <files>crates/loadorder/src/loot.rs, crates/loadorder/tests/libloot_spike.rs</files>
  <read_first>.planning/phases/02-multi-mod-management/02-RESEARCH.md (Pattern 2 lines 184-212, Pitfall 1 lines 274-278, Code Examples lines 304-325), crates/steam/src/resolve.rs (SKYRIM_SE/FALLOUT4 constants + the prefix-path style), crates/loadorder/src/error.rs</read_first>
  <behavior>
    - appdata_local_path(prefix, "Skyrim Special Edition") == <prefix>/drive_c/users/steamuser/AppData/Local/Skyrim Special Edition.
    - game_type_for(489830) == Some(SkyrimSE); game_type_for(377160) == Some(Fallout4); game_type_for(220) == None.
    - open_game against a fixture prefix (built by fake_proton_prefix) does NOT return NoLocalAppData (A1/A3 — the Linux seam works because we pass with_local_path).
    - After loading a small set of plugins and calling set_order_and_save with a masters-first order, the plugins.txt written at libloot's active_plugins_file_path is in asterisk-enabled format and reflects the order (D-07).
  </behavior>
  <action>
Implement the minimal libloot seam (D-07/D-09; RESEARCH Pattern 2). VERIFY the exact libloot 0.29 public API names against the crate docs (Context7 / docs.rs `libloot`) before coding — RESEARCH cites `Game::with_local_path`, `load_current_load_order_state`, `load_plugins`, `sort_plugins`, `set_load_order`, `is_plugin_active`, `active_plugins_file_path`, `GameType::{SkyrimSE,Fallout4}`; if a name differs in 0.29.5, adapt and record the actual name in the SUMMARY.

1. `fn appdata_local_path(prefix: &Path, game_name: &str) -> PathBuf` — join `drive_c/users/steamuser/AppData/Local/<game_name>` (Pitfall 1/2; the folder is "Skyrim Special Edition" / "Fallout4" for Steam, A3).
2. `fn game_type_for(appid: u32) -> Option<libloot::GameType>` — map the two supported appids; None otherwise.
3. `fn open_game(appid, install_dir, appdata_local) -> Result<libloot::Game, LoadOrderError>` — `game_type_for(appid).ok_or(...)`, then ALWAYS `Game::with_local_path(game_type, install_dir, appdata_local)` (NEVER `Game::new` — it returns NoLocalAppData on Linux). Map libloot errors into `LoadOrderError::Loot(e.to_string())`. Create the appdata_local parent dirs before constructing if absent (Pitfall 2: a never-launched game has no AppData/Local/<Game> yet).
4. `fn set_order_and_save(game: &mut libloot::Game, order: &[&str]) -> Result<(), LoadOrderError>` — `load_current_load_order_state`, `set_load_order(order)`, then persist (libloot's save/equivalent) so Plugins.txt is written. masters-first is enforced INTERNALLY by libloot — do NOT hand-roll it (D-08).

Spike test `crates/loadorder/tests/libloot_spike.rs`: build a fixture prefix via `testkit::fake_proton_prefix(root, "Skyrim Special Edition", None)`, place a couple of dummy plugin files in a fixture install Data/ dir (or whatever libloot's load_plugins needs — header-only dummies are fine), call `open_game(489830, &install, &appdata_local)`, exercise a load → set_order_and_save round-trip, then read the written `active_plugins_file_path()` and assert it lives under the fixture AppData/Local path and is asterisk-format. The PRIMARY assertion is that NoLocalAppData does NOT occur (A1/A3 de-risked). If libloot needs real ESM/ESP header bytes to load plugins, use the minimal valid TES4 header esplugin/libloot accepts, or restrict the spike to the load-order-state + path round-trip (set/save without parsing) and record the limitation in the SUMMARY — the non-negotiable goal is proving the with_local_path Linux seam.
  </action>
  <verify>
    <automated>cargo test -p nextwist-loadorder --test libloot_spike 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - The spike test passes, proving `with_local_path` works against a fixture prefix with NO NoLocalAppData error (A1/A3).
    - plugins.txt (or the load-order state, per the recorded limitation) round-trips at the libloot-reported path in asterisk format.
    - `cargo clippy -p nextwist-loadorder --all-targets -- -D warnings` clean.
    - SUMMARY records the EXACT libloot 0.29.5 API names used (so Plan 04 is mechanical) and any spike limitation.
  </acceptance_criteria>
  <done>The libloot Linux integration is proven against a controllable fixture; the with_local_path seam and asterisk plugins.txt write are verified; Plan 04 can build the full plugin manager on this wrapper.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NexTwist ↔ libloot (3rd-party GPL crate) | New external dependency entering the build/distribution graph. |
| NexTwist ↔ Proton prefix filesystem | plugins.txt is written inside the resolved Proton prefix (`drive_c/users/steamuser/AppData/Local/<GameName>`); a wrong path computation could write outside the intended prefix. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | libloot package install | mitigate | Blocking human-verify checkpoint before first install (SUS-on-low-downloads, approved as niche/authoritative); verified against crates.io + github.com/loot/libloot; pinned to 0.29; cargo-deny allows its GPL-3.0 (Plan 01). Never auto-approvable. |
| T-02-04 | Tampering | plugins.txt path computation | mitigate | `appdata_local_path` derives strictly from the resolved prefix root + the known game folder name; the spike asserts the written path stays under the fixture AppData/Local path (bounded inside the prefix). Plan 04 reuses this same bounded path. |
| T-02-05 | Information Disclosure | GPL-3.0 distribution implication | accept (tracked) | Recorded as a Phase-5 DIST-02 audit item; deny.toml already allows GPL-3.0-or-later. Not a runtime threat. |
</threat_model>

<verification>
- `cargo build -p nextwist-loadorder` resolves libloot and compiles.
- `cargo test -p nextwist-loadorder --test libloot_spike` green (A1/A3 de-risked).
- `cargo test -p nextwist-testkit fake_proton_prefix` green.
- `cargo deny check` still passes with libloot in the graph (GPL allowance present).
- `cargo clippy -p nextwist-loadorder --all-targets -- -D warnings` clean.
</verification>

<success_criteria>
- The single largest unknown (libloot on Linux, A1/A3) is proven against a fixture prefix: `with_local_path` works, no NoLocalAppData, asterisk plugins.txt write round-trips.
- A Tauri-free `crates/loadorder` crate exists with a thiserror error type and a verified libloot wrapper.
- testkit provides a reusable fixture-prefix builder.
- The GPL/SUS libloot install passed a blocking human checkpoint; cargo-deny stays green.
</success_criteria>

<output>
Create `.planning/phases/02-multi-mod-management/02-02-SUMMARY.md` when done, recording: the EXACT libloot 0.29.5 API surface used (method names/signatures), any spike limitation (e.g. needing real plugin headers), and the verified appdata_local_path shape — Plan 04 depends on these specifics.
</output>
