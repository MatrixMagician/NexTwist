//! `NexusClient` — the async reqwest + governor NexusMods API client.
//!
//! STUB (Plan 01): the metadata / download-link calls and the rate-limiter wiring land
//! in Plan 02. This module is declared now so the crate's public module layout is fixed
//! for the download slices to build on, per the headless/shell boundary contract.
